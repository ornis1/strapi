{
  "routes": [
    {
      "method": "GET",
      "path": "/catalog",
      "handler": "catalog.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/catalog",
      "handler": "catalog.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/catalog",
      "handler": "catalog.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
